name: Regularly check devnet

on:
    workflow_dispatch:
    schedule:
        - cron: "0 */2 * * *"

jobs:
    check-devnet:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/setup-python@v5
              with:
                  python-version: 3.11

            - uses: actions/checkout@v4

            - name: Install dependencies
              run: |
                  pip install -r ./requirements-dev.txt
                  curl -sSfL https://raw.githubusercontent.com/coinbase/mesh-cli/master/scripts/install.sh | sh -s -- -b "$HOME/.local/bin"
                  echo "$HOME/.local/bin" >> $GITHUB_PATH

            - name: Build
              run: |
                  cd $GITHUB_WORKSPACE/cmd/rosetta && go build .
                  cd $GITHUB_WORKSPACE/systemtests && go build ./proxyToObserverAdapter.go

            - name: check:data (shard 0)
              run: |
                  python3 ./systemtests/check_with_mesh_cli.py --mode=data --network=devnet --network-shard=0
              env:
                  PYTHONPATH: .

            - name: check:data (shard 1)
              run: |
                  python3 ./systemtests/check_with_mesh_cli.py --mode=data --network=devnet --network-shard=1
              env:
                  PYTHONPATH: .

            - name: check:data (shard 2)
              run: |
                  python3 ./systemtests/check_with_mesh_cli.py --mode=data --network=devnet --network-shard=2
              env:
                  PYTHONPATH: .
